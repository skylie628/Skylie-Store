const {body} = require('express-validator');

const add = () => {
    return [ 
     ]; 
  }

  const update = () => {
    return [ ]; 
  }

  const get = () => {
    return [ 
      
    ]; 
  }

  const drop = () => {
    return [ 
      
    ]; 
  }
  const checkAllow = () => {
    return [ 
      
    ]; 
  }
export const commentValidate = {
    add,update,get,drop,checkAllow
};
